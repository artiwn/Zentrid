# Plant Detail UX v119

Plant Detail now separates backend-managed and local prototype records, provides local edit mode for master/source data, validates capacity and relationships, protects unsaved changes, and displays honest device, telemetry, source and freshness states. Plant lifecycle status remains read-only and no backend update endpoint is assumed.
